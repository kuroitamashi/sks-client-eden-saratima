---
import { getTheme, getContent } from '../lib/theme.js';
import { listCategories } from '../lib/products.js';

const theme = await getTheme();
const content = await getContent();
const annee = new Date().getFullYear();
const rayons = (await listCategories()).map((r) => ({
  libelle: r.libelle,
  href: `/produits/?rayon=${r.cle}`,
}));
const logo = theme.identity.logoText ?? theme.identity.name;
---

<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <span class="footer__logo">{logo}</span>
      <p class="footer__tagline">{content.footer.texte}</p>

      <ul class="footer__contact">
        {theme.identity.whatsapp && (
          <li>
            <a class="link" href={`https://wa.me/${theme.identity.whatsapp.replace(/\D/g, '')}`} target="_blank" rel="noopener">
              {theme.identity.whatsapp}
            </a>
          </li>
        )}
        {theme.identity.email && (
          <li><a class="link" href={`mailto:${theme.identity.email}`}>{theme.identity.email}</a></li>
        )}
        {theme.identity.instagram && (
          <li>
            <a class="link" href={`https://instagram.com/${theme.identity.instagram}`} target="_blank" rel="noopener">
              @{theme.identity.instagram}
            </a>
          </li>
        )}
      </ul>
    </div>

    <div class="footer__columns">
      {content.footer.colonnes.map((col) => (
        <div class="footer__column">
          <h4 class="footer__column-title">{col.titre}</h4>
          <ul class="footer__links">
            {(col.avecRayons ? [...col.liens, ...rayons] : col.liens).map((lien) => (
              <li><a href={lien.href} class="link">{lien.libelle}</a></li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>

  <div class="container">
    <div class="footer__bottom">
      <p class="footer__copyright">© {annee} {theme.identity.name}</p>

      {content.footer.paiements?.length > 0 && (
        <ul class="footer__paiements" aria-label={content.footer.paiementsLibelle}>
          {content.footer.paiements.map((p) => (
            <li class:list={['pastille', { 'pastille--claire': p.claire }]}>
              <img src={p.logo} alt={p.nom} width="114" height="72" loading="lazy" decoding="async" />
            </li>
          ))}
        </ul>
      )}

      {content.footer.propulsePar && (
        <p class="footer__mentions">
          <span>{content.footer.propulsePar.texte}</span>
          <a class="link footer__sks" href={content.footer.propulsePar.url} target="_blank" rel="noopener">
            <img src={content.footer.propulsePar.logo} alt="" width="24" height="24" loading="lazy" decoding="async" />
            <span>{content.footer.propulsePar.nom}</span>
          </a>
        </p>
      )}
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-surface);
    padding-top: var(--section-space-l);
    /* Le bouton WhatsApp est fixe dans le coin bas droit, a environ 5 rem du
       bas de l'ecran. Sans cette reserve, arrive tout en bas de la page, il
       recouvre la mention SKS de la derniere rangee. */
    padding-bottom: calc(var(--space-8) + 3rem);
    margin-top: var(--section-space-m);
  }

  .footer__inner {
    display: grid;
    gap: var(--space-12);
    grid-template-columns: 1fr;

    @media (width >= 800px) {
      grid-template-columns: 1.4fr 1fr;
    }
  }

  .footer__logo {
    font-family: var(--font-display);
    font-weight: var(--font-display-weight);
    font-size: var(--h3);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    display: block;
    margin-bottom: var(--space-4);
  }

  .footer__tagline {
    color: var(--color-text-muted);
    max-width: 40ch;
  }

  .footer__contact {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    margin-top: var(--space-6);
    color: var(--color-text-muted);
  }

  .footer__columns {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-8);
  }

  .footer__column-title {
    font-family: var(--font-body);
    font-size: var(--text-m);
    font-weight: var(--weight-medium);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--color-text-muted);
    margin-bottom: var(--space-5);
  }

  .footer__links {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  /* Les logos de paiement, en petites cartes de meme format (38 x 24), comme
     chez Shopify : c'est le format commun qui fait une rangee propre, pas un
     logo posé dans une case.

     Le rayon est ecrit en dur : le template est en forme `net`, ou tous les
     rayons du contrat valent zero, alors qu'une carte a des coins arrondis.
     Le liseré n'existe que sur les cartes blanches (`claire`), pour les
     detacher du fond ; sur une carte coloree il ne servirait a rien. */
  .footer__paiements {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-2);
  }

  .pastille {
    position: relative;
    height: 2rem;
    aspect-ratio: 38 / 24;
    overflow: hidden;
    border-radius: 0.25rem;
  }

  /* Sur telephone, le format exact de Shopify (38 x 24) : a 51 px de large,
     sept cartes ne tiennent pas dans 350 px et Mastercard finissait seule sur
     une deuxieme rangee. */
  @media (width < 480px) {
    .pastille {
      height: 1.5rem;
    }
  }

  .pastille img {
    display: block;
    width: 100%;
    height: 100%;
  }

  .pastille--claire::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    box-shadow: inset 0 0 0 1px var(--color-border);
  }

  /* La rangee du bas : le copyright a gauche, les cartes de paiement AU
     CENTRE, la mention SKS a droite. Trois colonnes dont les deux latérales
     sont egales (1fr) : c'est ce qui centre vraiment les cartes, meme quand
     les deux textes n'ont pas la meme longueur.

     Sous 1000 px il n'y a pas la place pour trois blocs cote a cote (mesure
     au navigateur : la colonne de droite tombait a 147 px a 800 px, et la
     mention se cassait sur quatre lignes) : ils s'empilent, centres. */
  .footer__bottom {
    display: grid;
    grid-template-columns: 1fr;
    justify-items: center;
    gap: var(--space-5);
    text-align: center;
    margin-top: var(--space-16);
    padding-top: var(--space-6);
    border-top: 1px solid var(--color-border);
    color: var(--color-text-muted);

    @media (width >= 1000px) {
      grid-template-columns: 1fr auto 1fr;
      align-items: center;
      text-align: start;
    }
  }

  /* La mention SKS : le texte, puis le lien avec l'icone de la plateforme.
     L'icone est decorative (alt vide) : le nom ecrit a cote est deja la
     description du lien, la repeter dans l'alt la ferait lire deux fois. */
  .footer__mentions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
  }

  .footer__sks {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    color: var(--color-text);
    font-weight: var(--weight-medium);
  }

  .footer__sks img {
    height: 1.5rem;
    width: auto;
  }

  @media (width >= 1000px) {
    /* Empilee sur deux lignes, a droite : elle ne demande alors que ~190 px
       et ne change plus d'aspect selon la largeur. Sur une ligne elle en
       voulait 380, et se cassait des que l'ecran passait sous 1300 px. */
    .footer__mentions {
      justify-self: end;
      flex-direction: column;
      align-items: flex-end;
      gap: var(--space-1);
    }

    .footer__copyright {
      justify-self: start;
    }
  }
</style>
